MD013:
  line_length: 256            # Line length 80 is far to short
MD040: false                  # Allow ``` blocks in md files with no language specified
MD046: false                  # Temporarily disable Expected: indented; Actual: fenced

